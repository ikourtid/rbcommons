package com.rb.nonbiz.io.http;

/**
 * This is here as a reminder that we can't really test JsonApiCallerUsingPost, because
 * it's I/O. However, one nice thing about having it as a separate class is that we can mock JsonApiCallerUsingPost
 * in tests of verb classes that inject it.
 */
public class JsonApiCallerUsingPostTest {
}
