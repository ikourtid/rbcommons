package com.rb.nonbiz.testcategories;

/**
 * This is just a category marker, so it is intentionally empty.
 *
 * It is meant to separate research backtests (which document experiments done) from regular backtests,
 * so that we can exclude them from backtest runs.
 */

public interface ResearchBacktests {

}
