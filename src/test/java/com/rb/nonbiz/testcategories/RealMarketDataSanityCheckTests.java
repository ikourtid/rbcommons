package com.rb.nonbiz.testcategories;

/**
 * An intentionally empty category marker.
 *
 * Slow-ish tests to verify that our market data makes sense. Examples:
 * - do ETFs tracking the same (or similar) index show the same total returns every day vs each other?
 * - can we load all market data without throwing an exception?
 */
public interface RealMarketDataSanityCheckTests {

}
