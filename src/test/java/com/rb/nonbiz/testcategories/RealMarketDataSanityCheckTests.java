package com.rb.nonbiz.testcategories;

/**
 * An intentionally empty category marker.
 *
 * <p> Slow-ish tests to verify that our market data makes sense. Examples:
 * <ul>
 *   <li> do ETFs tracking the same (or similar) index show the same total returns every day vs each other? </li>
 *   <li> can we load all market data without throwing an exception? </li>
 * </ul>
 * </p>
 */
public interface RealMarketDataSanityCheckTests {

}
