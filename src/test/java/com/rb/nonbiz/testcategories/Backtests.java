package com.rb.nonbiz.testcategories;

/**
 * This is just a category marker, so it is intentionally empty.
 *
 * It is meant to separate backtests (which are more time-consuming) from regular tests,
 * so that we can selectively choose to run only backtests (or exclude them).
 */
public interface Backtests {

}
